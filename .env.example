# DeepSeek-Agent — API keys
# Add MULTIPLE keys: when one hits a rate limit or dies, the agent switches
# automatically and tells you, instead of stopping.
MISTRAL_API_KEY=
MISTRAL_API_KEY_2=
MISTRAL_API_KEY_3=

# Optional extra providers (enable in config/config.yaml first)
# OPENAI_API_KEY=
# GROQ_API_KEY=

# DEEPSEEK_APPROVAL_MODE=smart     # smart | always | never
# DEEPSEEK_THEME=cyber             # cyber | matrix | mono
# DEEPSEEK_WORKSPACE=./workspace
# DEEPSEEK_DEBUG=1
